{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "steps": {
    "install": {
      "inputs": [
        { "step": "packages:mise" },
        { "local": true }
      ],
      "commands": ["if [ -d app/src/main/assets/nodejs-project ]; then cd app/src/main/assets/nodejs-project; fi && npm install"]
    }
  },
  "deploy": {
    "startCommand": "if [ -d app/src/main/assets/nodejs-project ]; then cd app/src/main/assets/nodejs-project; fi && node server.js"
  }
}
